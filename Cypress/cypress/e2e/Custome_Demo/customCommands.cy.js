describe("Custom Commands", () => {
  it.only("Handling Links", () => {
    cy.visit("https://www.ebay.com");

    //Direct --without using custom commands
  });

  it("Over writing existing contains commands", () => {});
});
